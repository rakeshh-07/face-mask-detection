# face-mask-detection
Face Mask Detection  A deep learning–based Face Mask Detection system that identifies whether a person is wearing a mask or not in real-time using computer vision. The model is built using CNN and OpenCV to detect faces and classify them as Mask or No Mask with high accuracy.
